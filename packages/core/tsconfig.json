{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "module": "es2020",
    "types": ["jest", "vite/client"],
    "lib": ["esnext", "dom"],
    "outDir": "dist/types",
    "noEmit": false,
    "emitDeclarationOnly": true
  },
  "include": ["src/**/*.ts"],
  // hack so i don't need to change tests
  "exclude": ["src/**/*.test.ts", "src/client-test-utils.ts"]
}
